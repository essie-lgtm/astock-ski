/* =====================================================================
 * config.js —— 所有可调手感常量集中在这里
 * 改完刷新页面即可生效；也可在控制台 window.CONFIG.xxx 实时调。
 * 仅供娱乐 / not financial advice
 * ===================================================================== */
window.CONFIG = {
  /* ---------- 地形 ---------- */
  SEGMENT_WIDTH: 150,     // 每根K线的水平宽度(px)：调大=赛道更长、坡更缓更好骑（控时长主要靠它）
  LOG_TO_PX: 950,         // 每 1 个对数价格单位的纵向像素。用对数=任何价位的"涨跌%"都看得见，不再有长段假平地
  TERRAIN_AMP: 1.0,       // 地形纵向放大系数（乘在 LOG_TO_PX 上）
  GROUND_FILL_DEPTH: 4000,// 雪面以下填充深度(px)，纯渲染用
  OHLC_AMP: 1.0,          // 日线 OHLC 影线起伏夸张：每天的「最高/最低」相对收盘的落差被画成地形上下影。0=退回收盘单线(无日内起伏)；>1=日内颠簸更夸张。嫌平就调大，嫌老被小包弹起就调小

  /* ---------- 大事件纵向夸张（涨停发射墙 / 跌停悬崖）---------- */
  BIG_EVENT_GAIN: 2.4,    // 大事件当天纵向落差的放大倍数。【持续保留】不是对称小包→涨上去就待在上面、下来才有真跌幅
  BIG_EVENT_REL_CAP: 1.25,// 幅度/板幅 封顶（仅用于渲染强度与屏幕反馈缩放）
  DETREND_STRENGTH: 0.5,  // 去趋势强度：1=把整体大涨/大跌斜坡完全拉平(赛道水平起伏)；0=忠实保留趋势(下跌票=一路俯冲)；中间值折中
  DETREND_DOWN_STRENGTH: 0.15,// (A+C·非对称去趋势) 净下跌票专用：远小于上面=保留"从云端到套牢"的俯冲长坡；净上涨票仍用 DETREND_STRENGTH 免得变成无尽爬坡

  /* ---------- 跳水段（连续阴跌识别 · 白马崩盘的"千刀万剐"打包成可见长崖）----------
   * 单日大事件只认"今天暴跌"，但 43% 的崩盘常由无数 -2%/-3% 的平凡阴跌累积而成、单日都不达标。
   * 这里识别"从近峰深度回撤且仍在跌"的连续区，把那片缓坡局部陡化成可俯冲的长崖，并染冷色/打标签。
   */
  DIVE_DD_PCT: 10,        // 从近期最高价回撤≥此% 才可能算跳水
  DIVE_FALL_WIN: 5,       // 且最近这么多交易日收盘仍在走低 → 判定为跳水段（两条件同时满足）
  DIVE_GAIN: 1.5,         // 跳水段内"下跌日"纵向落差的放大倍数：长阴跌坡变陡、能真正俯冲；不与单日大事件(BIG_EVENT_GAIN)叠加
  DIVE_MIN_RUN: 4,        // 连续≥此天数的跳水段才打"跳水段"标签（避免碎段刷屏，陡化本身不受此限）

  /* ---------- 地形平滑（可骑性核心）---------- */
  TERRAIN_SUBSTEPS: 10,   // 相邻交易日之间补的过渡点数（越大越顺滑）
  TERRAIN_SMOOTH_WINDOW: 2,// 普通日均线半窗(±N日)压日间抖动；0=关闭，仅靠样条
  BIG_EVENT_PCT: 7,       // |当日涨跌幅%|≥此值视为大事件，不平滑、保留陡坡
  LAUNCH_CONVEXITY_MIN: 0.014,// 起跳所需最小凸度(rad/px)：低于此的缓坡/中等起伏一律贴地滑过，只有陡墙级才弹
  LAUNCH_SENSITIVITY: 1.1,// 起跳灵敏度：越大越不容易弹起（再叠加速度判定）

  /* ---------- 重力 / 自限速速度系统 ----------
   * 模型：引擎维持平地巡航速度，沿坡重力做坡度增速，二次阻力随速度增大→收敛到终端速度，
   *      maxSpeed 硬上限绝不超过。默认偏保守(宁可慢)；想更快调大 CRUISE_SPEED / 调小 DRAG_QUAD。
   */
  GRAVITY: 1750,          // 重力加速度 (px/s^2)
  CRUISE_SPEED: 480,      // 巡航速度：平缓地形稳定在此(px/s)≈58km/h
  SLOPE_ACCEL_K: 0.30,    // 坡度增速系数（已调小）：下坡加速 / 上坡减速的强度
  DRAG_QUAD: 0.0007,      // 二次阻力系数：速度越大阻力越大，自然收敛到终端速度、不会越滑越快
  MAX_SPEED: 1050,        // 最高速度硬上限(px/s)≈126km/h，任何情况都不超过（含腾空）
  MIN_SPEED: 280,         // 上坡最低速度(px/s)，不卡死
  START_SPEED: 420,       // 出发速度(px/s)

  /* ---------- 起跳（点按）---------- */
  JUMP_FORCE: 820,        // 点按起跳的离地上升速度(px/s)，即时跟手

  /* ---------- 空中转动 / 落地判定 ---------- */
  AIR_SPIN_SPEED: 10.0,   // 空中按住时转速(rad/s)，约 1.6 圈/秒（后空翻）
  AIR_SPIN_DIR: -1,       // 转向：-1 后空翻 / +1 前空翻
  AIR_BALANCE_EASE: 9.0,  // 松手时自然平衡：板面回正速度
  LAND_TOLERANCE_DEG: 36, // 落地容错角：板面与坡面夹角小于此=干净落地
  NO_TRICK_SPIN: 0.7,     // 空中累计转动小于此(rad,~40°)视为没做特技→落地永远算干净，不会摔
  GROUND_ANGLE_EASE: 14,  // 落地后板面贴合坡面的速度

  /* ---------- 摔倒（短暂翻滚后自动爬起，绝不结束游戏）---------- */
  CRASH_DURATION: 1.0,    // 摔倒翻滚基准时长(秒)，实际 0.85~1.2 倍随机
  CRASH_SPEED_KEEP: 0.32, // 摔倒瞬间保留的速度比例
  CRASH_CRAWL_SPEED: 130, // 翻滚期间掉到的爬行速度(px/s)，给雪崩留追击空间
  CRASH_DRAG: 4.0,        // 翻滚期间向爬行速度收敛的快慢

  /* ---------- 计分 ---------- */
  SCORE_PER_PX: 0.10,     // 每前进 1px 的基础分
  SCORE_PER_FLIP: 120,    // 每完成一圈空翻
  SCORE_CLEAN_LAND: 60,   // 每次干净落地
  COMBO_AIRTIME: 0.28,    // 滞空超过此秒数的落地才算一次连击(s)

  /* ---------- 相机 ---------- */
  CAM_X_RATIO: 0.33,      // 骑手在屏幕水平方向的位置比例
  CAM_Y_RATIO: 0.50,      // 骑手在屏幕垂直方向的位置比例
  CAM_LERP: 0.12,         // 相机跟随平滑
  CAM_LOOKAHEAD: 0.16,    // 前瞻秒数：相机沿前进方向前移 speed*此值，高速看更远
  CAM_MIN_ZOOM: 0.82,     // 高速时镜头最小缩放(拉远)，1=不缩放；越小拉得越远

  /* ---------- 大事件屏幕反馈（让你"感觉到"大起大落）---------- */
  FEEDBACK_PCT: 10,       // |当日涨跌幅%|≥此值才触发镜头/闪色反馈
  TOAST_DURATION: 2.2,    // 事件提示在屏幕上停留秒数（与速度无关，滑多快都读得清）；多条事件排队逐条显示
  HITSTOP_DUR: 0.28,      // 路过涨停/跌停时的"微慢动作"持续秒数（高光时刻，看清字+有戏剧性）
  HITSTOP_SCALE: 0.35,    // 微慢动作期间时间流速（0.35=慢到三分之一）；1=关闭慢动作
  FEEDBACK_PUNCH: 0.16,   // 镜头猛推强度（瞬间放大比例），按幅度缩放
  FEEDBACK_SHAKE: 14,     // 镜头抖动幅度(px)，按幅度缩放
  FEEDBACK_FLASH: 0.42,   // 闪色强度(0~1 透明度)，涨=金 跌=绿
  FEEDBACK_DECAY: 4.5,    // 反馈衰减速度（越大消失越快）

  /* ---------- 熊市雪崩 · 追杀机制 ----------
   * 速度全部用"巡航速度的倍数"表示，所以你以后改 CRUISE_SPEED 等速度，追杀松紧自动跟着缩放，不用重调。
   */
  AVALANCHE_ENABLED: true,     // 总开关。想纯爽滑：设 false，或访问时加 ?bear=0
  AVALANCHE_START_BACK: 900,   // 出发时雪崩在身后多远(px)
  AVALANCHE_BASE_FRAC: 0.78,   // 基础前压速度 = 此值 × 巡航速度（决定你停下/摔倒时多快被吞）
  AVALANCHE_SURGE_FRAC: 1.0,   // 浪涌系数 k：danger=1 时再叠加 此值 × 巡航速度
  DANGER_SMOOTH_WINDOW: 2,     // danger 用最近 N 根K线平滑
  // 牵引绳：danger 越高，允许落后越小 → 下跌日把熊拽进视野(明显逼近)，上涨/普通日放绳(被甩开)
  AVALANCHE_FAR_GAP: 1800,     // danger=0 时最多被甩开多远(px)
  AVALANCHE_NEAR_GAP: 170,     // danger=1 时贴到多近(px)
  DANGER_GAP_CURVE: 0.6,       // danger→收绳的曲线指数(<1 让中等跌幅就明显收绳)
  AVALANCHE_LEASH_RATE: 2.6,   // 收/放绳速度
  AVALANCHE_WARN_DIST: 560,    // 雪崩进入此距离(px)开始红色预警
  AVALANCHE_CATCH_MARGIN: 24,  // 雪崩前沿越过骑手身位多少算"贴上"
  CAUGHT_GRACE_FRAMES: 30,     // 贴上后还能撑的容错帧数，超过=被吞(game over)
  AVALANCHE_WAVE_HEIGHT: 240,  // 雪崩浪高(px)，渲染用
};
